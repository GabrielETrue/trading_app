[project]
name = "trading_app"
version = "0.1.0"
description = "Tracking trading, creating models, and displaying results"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "matplotlib>=3.10.8",
  "numpy>=2.0",
  "pandas>=2.2",
]
